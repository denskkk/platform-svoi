# 🎯 Система Заявок на Послуги

## Огляд Системи

Повністю функціональна система для подання заявок на послуги, їх обробки та оплати через УЦМ.

---

## 📋 Основний Функціонал

### Для Клієнтів (Замовників)

1. **Подання Заявки**
   - Кнопка "Подати заявку" на сторінці послуги
   - Форма створення заявки з деталями
   - Автоматичне підставлення даних з послуги
   - Вказання бюджету та термінів

2. **Управління Заявками**
   - Перегляд всіх своїх заявок
   - Відстеження статусу
   - Редагування до прийняття виконавцем
   - Скасування заявки

3. **Оплата**
   - Оплата через УЦМ після виконання
   - Автоматичне списання з балансу клієнта
   - Зарахування на баланс виконавця

4. **Відгуки**
   - Можливість залишити відгук після оплати

### Для Виконавців (Надавачів Послуг)

1. **Перегляд Заявок**
   - Доступні заявки (нові)
   - Заявки в роботі
   - Історія заявок

2. **Робота із Заявками**
   - Прийняття заявки із вказанням ціни
   - Відхилення заявки
   - Переведення в статус "Виконується"
   - Завершення роботи

3. **Отримання Оплати**
   - Отримання УЦМ після оплати клієнтом
   - Автоматичні сповіщення

---

## 🔄 Життєвий Цикл Заявки

```
1. NEW (Нова) 
   ↓ Виконавець переглядає
2. VIEWED (Переглянута)
   ↓ Виконавець приймає + вказує ціну
3. ACCEPTED (Прийнята в роботу)
   ↓ Виконавець починає роботу
4. IN_PROGRESS (Виконується)
   ↓ Виконавець завершує роботу
5. COMPLETED (Виконана)
   ↓ Клієнт оплачує через УЦМ
6. PAID (Оплачена)
   ↓ Можливість залишити відгук
```

**Альтернативні шляхи:**
- `CANCELLED` - клієнт скасував заявку
- `REJECTED` - виконавець відхилив заявку

---

## 🛠️ Технічна Реалізація

### База Даних

**Таблиця:** `service_requests`

**Основні поля:**
- `id` - ID заявки
- `client_id` - ID клієнта (хто створив заявку)
- `executor_id` - ID виконавця (хто прийняв заявку)
- `service_id` - ID послуги (якщо заявка на конкретну послугу)
- `title` - Назва заявки
- `description` - Опис роботи
- `status` - Статус заявки
- `budget_from/budget_to` - Бюджет клієнта
- `agreed_price` - Узгоджена ціна (встановлює виконавець)
- `is_paid` - Чи оплачена заявка

**Статуси:** new, viewed, accepted, in_progress, completed, paid, cancelled, rejected

### API Endpoints

#### POST `/api/service-requests`
Створення нової заявки

**Body:**
```json
{
  "title": "Назва заявки",
  "description": "Опис роботи",
  "budgetMin": 100,
  "budgetMax": 500,
  "city": "Київ",
  "serviceId": 123  // опціонально
}
```

#### GET `/api/service-requests`
Отримання списку заявок з фільтрами

**Query параметри:**
- `type=my` - мої заявки (створені мною)
- `type=available` - доступні заявки (для виконавців)
- `type=assigned` - заявки в роботі (де я виконавець)
- `status=new` - фільтр по статусу
- `serviceId=123` - заявки на конкретну послугу

#### GET `/api/service-requests/[id]`
Деталі конкретної заявки

#### PATCH `/api/service-requests/[id]`
Оновлення заявки / зміна статусу

**Body приклади:**

Прийняти заявку:
```json
{
  "action": "accept",
  "agreedPrice": 300
}
```

Почати роботу:
```json
{
  "action": "start"
}
```

Завершити роботу:
```json
{
  "action": "complete"
}
```

Скасувати заявку:
```json
{
  "action": "cancel"
}
```

#### POST `/api/service-requests/[id]/pay`
Оплата заявки через УЦМ

**Умови:**
- Статус має бути `completed`
- Клієнт повинен мати достатньо УЦМ
- Виконавець повинен бути призначений

**Дія:**
- Списання УЦМ з клієнта
- Зарахування УЦМ виконавцю
- Зміна статусу на `paid`
- Створення транзакцій в `ucm_transactions`

---

## 🎨 UI Компоненти

### Сторінки

1. **`/services/[id]`** - Сторінка послуги
   - Кнопка "Подати заявку"
   - Показує "Заявка подана" якщо вже є активна заявка

2. **`/requests/create`** - Форма створення заявки
   - Підтримка параметрів: `?serviceId=123&executorId=456`
   - Автозаповнення даних з послуги
   - Поля для назви, опису, бюджету, локації

3. **`/service-requests`** - Список заявок
   - Фільтри: Доступні / Мої заявки / В роботі / Всі
   - Картки заявок з статусами
   - Швидкий перегляд інформації

4. **`/service-requests/[id]`** - Детальна сторінка заявки
   - Повна інформація про заявку
   - Контактні дані клієнта/виконавця
   - Кнопки дій в залежності від статусу та ролі
   - Історія змін статусу

### Візуальні Статуси

- 🟡 NEW - Жовтий
- 🔵 VIEWED - Синій
- 🟣 ACCEPTED - Фіолетовий
- 🟠 IN_PROGRESS - Помаранчевий
- 🟢 COMPLETED - Зелений
- 💚 PAID - Смарагдовий
- ⚫ CANCELLED - Сірий
- 🔴 REJECTED - Червоний

---

## 💰 Інтеграція з УЦМ

### Оплата Заявки

1. Клієнт натискає "Оплатити [ціна] УЦМ"
2. Система перевіряє баланс клієнта
3. Списується вказана сума з клієнта
4. Зараховується на баланс виконавця
5. Створюються 2 транзакції:
   - DEBIT для клієнта
   - CREDIT для виконавця
6. Статус змінюється на PAID
7. Створюється сповіщення виконавцю

### Транзакції

```typescript
// Списання з клієнта
{
  userId: clientId,
  kind: 'debit',
  amount: agreedPrice,
  reason: 'service_request_payment',
  relatedEntityType: 'ServiceRequest',
  relatedEntityId: requestId
}

// Зарахування виконавцю
{
  userId: executorId,
  kind: 'credit',
  amount: agreedPrice,
  reason: 'service_request_received',
  relatedEntityType: 'ServiceRequest',
  relatedEntityId: requestId
}
```

---

## 🔔 Сповіщення

Автоматичні сповіщення створюються при:

1. **Прийнятті заявки** (клієнту)
   - "Виконавець прийняв вашу заявку"
   
2. **Завершенні роботи** (клієнту)
   - "Виконавець завершив роботу. Будь ласка, оплатіть"
   
3. **Оплаті** (виконавцю)
   - "Клієнт оплатив заявку. Ви отримали [сума] УЦМ"

---

## ✅ Переваги Системи

1. **Прозорість** - всі етапи роботи відстежуються
2. **Безпека** - оплата тільки після завершення
3. **Зручність** - простий інтерфейс для обох сторін
4. **Інтеграція** - використання внутрішньої валюти УЦМ
5. **Відгуки** - можливість оцінити роботу
6. **Історія** - збереження всіх заявок та транзакцій

---

## 📱 Як Користуватися

### Для Клієнта

1. Знайдіть потрібну послугу
2. Натисніть "Подати заявку"
3. Заповніть форму з деталями
4. Чекайте на відповідь виконавця
5. Після завершення роботи - оплатіть через УЦМ
6. Залиште відгук

### Для Виконавця

1. Перегляньте доступні заявки
2. Оберіть підходящу заявку
3. Натисніть "Прийняти заявку" та вкажіть ціну
4. Почніть роботу
5. Після завершення натисніть "Завершити роботу"
6. Отримайте оплату в УЦМ

---

## 🚀 Готово до Використання

Система повністю функціональна та готова до продакшену:

✅ База даних налаштована  
✅ API endpoints реалізовані  
✅ UI компоненти створені  
✅ Інтеграція з УЦМ працює  
✅ Сповіщення налаштовані  
✅ Відгуки інтегровані  

**Запуск:**
```bash
npm run build
pm2 restart sviy-web
```

**Перевірка:**
1. Відкрийте будь-яку послугу
2. Натисніть "Подати заявку"
3. Перевірте роботу всього циклу

---

## 📞 Підтримка

При виникненні питань або проблем:
- Перевірте логи: `pm2 logs sviy-web`
- Перевірте базу даних: `psql -U sviy_user -d sviy_db`
- Перевірте Prisma: `npx prisma studio`
